export interface GatewayIdProperty {
  id: string;
}

export interface IntegrationIdProperty {
  integrationId: string;
}

export interface LabelProperty {
  label: string;
}

export interface DescriptionProperty {
  description: string;
}

export interface DefaultProperty {
  default: boolean;
}

export interface LiveProperty {
  live: boolean;
}
